/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "encrypted-tbn0.gstatic.com",
      "cdn.pixabay.com",
      "www.simplilearn.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
